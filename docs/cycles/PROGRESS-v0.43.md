# PROGRESS-v0.43.md — append-only execution record

This file records v0.43 task and gate events after their implementation
commits exist. Each entry names the measured result, every applicable
acceptance criterion, the golden delta, and the real implementation commit.
Entries are append-only; corrections are new dated entries.

### 2026-08-05 · ACTIVATE — v0.43 cycle activation

- owner: Codex
- runbook: `TASKS-v0.43-EXECUTION.md`
- commit: d8a9b83a73d7f106308d9d02d45001d69a501e66
- declaration acceptance: PASS. The first post-activation `cycle-check`
  resolves v0.43 from `AGENTS.md` alone and reports `state=open`, verified
  local tag references, declared scope and trigger freshness accepted, both
  artifact boundaries and State regions bound, and every prior cycle closed.
- governed-row acceptance: PASS. All **31/31** deferral subjects carry dated
  v0.43 observations measured before the declaration moved; all four
  trigger-bearing Architecture rows name v0.43 and no template survives.
- pre-activation contract acceptance: PASS. The delivered semantic
  `## Cycle closing record` heading was renamed to the established
  non-semantic assembly template before its first commit. The staged lifecycle
  result then contained only the two predicted first-commit/activation-anchor
  defects, and the first committed run passes.
- retention acceptance: PASS. The configured pattern derives exactly
  v0.42–v0.43 at the unchanged depth of two. Exact activation commit
  `d8a9b83a…` exports **2,691,054 bytes / 157 tracked files / 2 retained
  cycles**, with all **157/157** required paths present and every derived
  exclusion absent.
- governed review-export measurement: tree=`d8a9b83a73d7f106308d9d02d45001d69a501e66`; bytes=`2691054`
- boundary acceptance: PASS. Activation is **121,666 bytes above** the
  2,569,388-byte attention boundary and leaves **308,946 bytes / 10.30% /
  1.43 high-water cycles** below the unchanged ceiling, a **19,674-byte
  decrease** from v0.42's governed value. Grants E and H remain separate
  unheld **84,896-byte** and **88,665-byte** levers.
- artifact acceptance: PASS. State was **221,818 / 453,741 bytes** before the
  activation record and is **224,369 bytes** in the exact activation commit.
  Manifest schema v2 remains **3 artifacts / 339 pins** at **200,440 /
  1,048,576 bytes**; two complete checks took **0.10 s / 0.10 s real**.
- executable acceptance: PASS. R15 reports **0 differences / 6 routes / 112
  field occurrences**; `version-check` derives **3** offline pins, **22** floor
  restatements, **3** release restatements, and classifies **593** tracked
  files; the registered self-test passes **17/17 rules / 119 controls**.
  Permission-capable Python 3.11 and Python 3.12 each pass **397/397** with the
  accepted warning non-fatal.
- remote acceptance: PASS. Permission-capable R17 readback kept
  `main=993813c755e9f759a4ee165954c7a1df984f6b10` and every expected/absent ref
  in agreement. No ref moved.
- grant acceptance: PASS. Grants E, G, and H were not issued. No dormant
  archive, retention-depth change, or publication branch executed.
- protected-input acceptance: PASS. The three historical amendment inputs
  remain untouched and untracked. No dependency, production source, protected
  byte, observation, fixture, publisher wire, entitlement/licensing outcome,
  local tag, branch, or remote ref moved.
- stop conditions: none. The sandboxed export and Python failures were
  disclosed non-measurements; their permission-capable reruns passed.
- golden-E2E delta: **0**. The permission-capable run passed **11/11**.

### 2026-08-05 · E0 — entering-state reconstruction

- owner: Codex
- commit: 1c4c99b4576224ea38b23d93c059c92316bea8c5
- hypothesis acceptance: PASS. All **8/8** hypotheses now carry dated
  executed verdicts: H1–H6 and H8 are confirmed; H7 is partly confirmed and
  partly refuted by the raw file's one additional terminal-newline byte.
- topology acceptance: PASS. Local object reads prove annotated object
  `a7852c55…` → closing commit `0382622b…` → release parent `54523559…`, with
  entering audit child `6f07edf8…` as its immediate child. Direct remote reads
  prove `main=993813c7…`, v0.17.7/v0.17.8 present, v0.17.6/v0.17.9 absent, and
  **16** unpublished first-parent commits. No ref moved.
- detached-checkout acceptance: PASS. Real `cycle-check` at annotated v0.17.9
  passed the closed v0.42 tree with verified local tag references and governed
  export binding.
- export acceptance: PASS. Exact entering export is **2,732,434 bytes / 157
  tracked entries / 2 retained cycles**; retained cycle documents are
  **178,284 bytes / 6.52%**. It is **163,046 bytes above** attention and leaves
  **267,566 bytes / 8.92% / 1.24 high-water cycles** below the ceiling.
- ledger-input acceptance: PASS. Both retained tables carry **31** subjects;
  their sections are **11,124** and **11,294** bytes. Source tracing confirms
  only carry-forward semantically reads the prior runbook; export membership
  is not an input to that comparison.
- artifact acceptance: PASS. Raw entering State is **221,818 / 453,741 bytes**
  and its directly delimited archival lever is **84,896 bytes**. The registry
  derives **17 rules / 119 controls / 9 exemptions / 3 retractions**; checklist
  reconciliation passes **333 checked / 3 retracted / 324 matched / 324
  resolved / 9 exemptions**.
- executable acceptance: PASS. Full `./run ci-local` passed **22/22** jobs,
  including **17/17 invariant rules / 119 controls**, clean default, net,
  MSRV, clippy, and format lanes. Fresh Python 3.11.4 and 3.12.13 environments
  each passed constraints and **397/397 tests** with the accepted warning.
- protected-input acceptance: PASS. The three pre-existing untracked amendment
  inputs remain untouched; no dependency, production source, protected byte,
  local tag, branch, or remote ref moved.
- stop conditions: none.
- golden-E2E delta: **0**. The standalone post-record run and full matrix each
  passed **11/11**.

### 2026-08-05 · E0 — runbook-qualification correction

- owner: Codex
- runbook: `TASKS-v0.43-EXECUTION.md`
- commit: 1c4c99b4576224ea38b23d93c059c92316bea8c5
- correction: the original E0 audit entry omitted the v0.36-forward explicit
  runbook qualifier. This append-only correction supplies that qualifier; it
  does not replace or change any earlier measurement.
- acceptance: PASS. E0's implementation commit remains the same real commit,
  and all measurements and acceptance results remain in the preceding entry.
- golden-E2E delta: **0**; this correction changes only audit prose.
