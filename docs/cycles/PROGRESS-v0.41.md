# PROGRESS-v0.41.md — append-only execution record

This file records v0.41 task and gate events after their implementation
commits exist. Each entry names the measured result, every applicable
acceptance criterion, the golden delta, and the real implementation commit.
Entries are append-only; corrections are new dated entries.

### 2026-08-04 · ACTIVATE — v0.41 cycle activation

- owner: Codex
- runbook: `TASKS-v0.41-EXECUTION.md`
- commit: 53f1595b0aa8af23ed01a0aa6000bc255326eba5
- declaration acceptance: PASS. The first post-activation `cycle-check`
  resolves v0.41 from `AGENTS.md` alone and reports `state=open`, with local
  tag reconciliation verified, declared scope and trigger freshness accepted,
  both artifact boundaries and State regions bound, and every prior cycle
  closed.
- governed-row acceptance: PASS. Direct table derivation finds **31/31**
  deferral subjects, every one with a dated v0.41 observation; the four
  trigger-bearing Architecture rows also name v0.41. No template survives.
- pre-activation contract correction: PASS. The delivered semantic
  `## Cycle closing record` template was renamed before the runbook's first
  commit, so the open cycle cannot masquerade as closed. The operator's exact
  initiating publication authorization is recorded as “I authorize you to
  publish before you begin the current task.” DR33 now applies that authority
  to v0.17.7 and the v0.41 release while preserving permanent withholding of
  v0.17.6 and every R-CLOSE precondition.
- grant acceptance: PASS. Publication authority does not supply Grant E. No
  structural archive or protected-manifest byte moved, and Step 5 remains
  dormant pending the grant's exact required content.
- retention acceptance: PASS. The configured pattern derives exactly
  v0.40–v0.41. The exact activation commit exports **2,771,866 bytes / 158
  tracked files / 2 retained cycles** with all **158/158** required paths
  present, all **4** raw-wire bodies and both structural archives absent, and
  no untracked export entry. The net change from exact entering commit
  **2,803,926 bytes** is **−32,060 bytes** after adding the active cycle and
  retiring v0.39 from review retention.
- governed review-export measurement: tree=`53f1595b0aa8af23ed01a0aa6000bc255326eba5`; bytes=`2771866`
- boundary acceptance: PASS. Exact activation is **202,478 bytes above** the
  2,569,388-byte attention boundary and leaves **228,134 bytes / 7.60% / 1.06
  high-water cycles** below the unchanged 3,000,000-byte ceiling. The
  attention predicate remains fired and disposed under the entering rule.
- artifact acceptance: PASS. Manifest schema v2 remains **3 artifacts / 339
  pins** at **200,440 / 1,048,576 bytes**; two complete checks took **0.10 s /
  0.10 s real**. Exact activation State is **175,998 / 453,741 bytes**.
- executable acceptance: PASS. R15 reports **0 differences / 6 routes / 112
  field occurrences**, `version-check` derives **3** offline pins / **22**
  floor restatements / **3** release restatements across **589** tracked files,
  and the registered suite passes **16/16 rules / 108 controls**.
- protected-input acceptance: PASS. The three historical amendment inputs
  remain untouched and untracked. No dependency, production source, protected
  byte, observation, fixture, publisher wire, entitlement/licensing outcome,
  local tag, or remote ref moved.
- stop conditions: none. The pre-commit lifecycle result contained only the
  two predicted uncommitted-runbook/activation-anchor defects; the first
  committed run passes.
- golden-E2E delta: **0**. The sandbox-denied bind was a non-result; the
  permission-capable identical command passed **11/11**.
