# PROGRESS-v0.40.md — append-only execution record

This file records v0.40 task and gate events after their implementation
commits exist. Each entry names the measured result, every applicable
acceptance criterion, the golden delta, and the real implementation commit.
Entries are append-only; corrections are new dated entries.

### 2026-08-04 · ACTIVATE — v0.40 cycle activation

- owner: Codex
- runbook: `TASKS-v0.40-EXECUTION.md`
- commit: 6896f6e3168bee0cb999ab2cfeca68cc715a681d
- declaration acceptance: PASS. The first post-activation `cycle-check`
  resolves v0.40 from `AGENTS.md` alone and reports `state=open`, with local
  tag reconciliation, declared scope, trigger freshness, artifact boundaries,
  State regions, and all prior cycles accepted.
- governed-row acceptance: PASS. Direct table derivation finds **31/31**
  deferral subjects, every one with a dated v0.40 observation; the four
  trigger-bearing Architecture rows also name v0.40. No template survives.
- author-contract correction: PASS before semantic acceptance. The draft's
  literal `## Cycle closing record` repeated the already-measured v0.39 open-
  cycle defect. It is now the non-semantic `## Closing-record assembly
  template`; no checker predicate was weakened.
- retention acceptance: PASS. The derived configuration advances from
  v0.38–v0.39 to exactly v0.39–v0.40. The staged worktree check passed at
  **2,710,665 bytes / 158 tracked files / 2 retained cycles**; the exact
  activation commit passes at **2,712,884 bytes / 158 tracked files / 2
  retained cycles**. All **158/158** required paths are present; the exact
  four derived raw-wire bodies and both structural archives are absent.
- governed review-export measurement: tree=`6896f6e3168bee0cb999ab2cfeca68cc715a681d`; bytes=`2712884`
- boundary acceptance: PASS. The exact activation commit leaves **287,116
  bytes / 9.57% / 2.68 cycles** below the unchanged 3,000,000-byte ceiling at
  the entering +107,226-byte denominator and is **72,664 bytes** below the
  entering 2,785,548-byte attention boundary. State is **155,221 / 453,741
  bytes**; the manifest is **200,440 / 1,048,576 bytes**.
- artifact acceptance: PASS. Two complete checks match **3 artifacts / 339
  pins** in **0.11 s / 0.09 s real**; neither manifest trigger clause fired.
- protected-input acceptance: PASS. The three historical amendment inputs
  remain byte-untouched and untracked. No dependency, production source,
  protected byte, observation, fixture, publisher wire, entitlement/licensing
  outcome, or remote ref moved.
- stop conditions: none. The pre-activation **2,788,376-byte** rejection was
  the predicted untracked/out-of-retention state and was not accepted.
- golden-E2E delta: **0**. The sandbox-denied bind was a non-result; the
  permission-capable identical command passed **11/11**.
